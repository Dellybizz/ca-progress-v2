import {NextResponse,type NextRequest} from "next/server";import {getProfileForUser,optionalUser} from "@/lib/auth/server";import {getPhase8NotificationCenter,markPhase8NotificationRead} from "@/lib/planner/phase8";import {MOBILE_API_HEADERS} from "@/lib/mobile/contract";import {nativeCorsHeaders,nativeOptions} from "@/lib/auth/native-cors";
export const dynamic="force-dynamic";const json=(r:NextRequest,d:unknown,s=200)=>NextResponse.json(d,{status:s,headers:{...MOBILE_API_HEADERS,...nativeCorsHeaders(r),"Cache-Control":"private, no-store"}});export const OPTIONS=(r:NextRequest)=>nativeOptions(r);
export async function GET(request:NextRequest){const user=await optionalUser();if(!user)return json(request,{error:{code:"AUTH_REQUIRED",message:"Authentication required.",retryable:false}},401);const profile=await getProfileForUser(user.id);const center=await getPhase8NotificationCenter(user.id,profile?.timezone||"Asia/Kolkata");return json(request,{schema:1,notifications:center.notifications});}
export async function POST(request:NextRequest){const user=await optionalUser();if(!user)return json(request,{error:{code:"AUTH_REQUIRED",message:"Authentication required.",retryable:false}},401);const body=await request.json().catch(()=>null) as {id?:unknown}|null;return json(request,await markPhase8NotificationRead(user.id,typeof body?.id==="string"?body.id:null));}
