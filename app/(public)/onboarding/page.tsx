import { PlaceholderPage } from "@/components/states/placeholder-page";

export default function OnboardingPlaceholderPage() {
  return (
    <PlaceholderPage
      eyebrow="Public/Auth · Phase 0"
      title="Onboarding boundary prepared."
      description="Course, group, attempt and study-target onboarding logic is intentionally deferred until the dedicated authentication/onboarding phase."
    />
  );
}
