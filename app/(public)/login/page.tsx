import { PlaceholderPage } from "@/components/states/placeholder-page";

export default function LoginPlaceholderPage() {
  return (
    <PlaceholderPage
      eyebrow="Public/Auth · Phase 0"
      title="Login boundary prepared."
      description="Authentication UI and provider logic deliberately begin in Phase 2. This route exists now so the architecture is stable from day one."
    />
  );
}
