import { PlaceholderPage } from "@/components/states/placeholder-page";

export default function DashboardPage() {
  return (
    <PlaceholderPage
      eyebrow="Student · Phase 0"
      title="A clean V2 student workspace starts here."
      description="This is an isolated staging shell. No legacy dashboard code, progress data or production database connection is present."
      stateTitle="Dashboard intentionally empty"
      stateBody="Phase 1 defines the design language. Later phases add authenticated student features and data."
    />
  );
}
