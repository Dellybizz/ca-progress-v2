export const MOBILE_BUILD = Object.freeze({
  applicationId: "in.zanisheluxe.caprogress",
  build: 2,
  apiVersion: 1,
  academicContextVersion: 1,
  localSchemaVersion: 0,
  channel: "internal",
});

export type MobileBuild = typeof MOBILE_BUILD;
