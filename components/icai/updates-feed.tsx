import { Badge } from "@/components/ui/badge";
import { EmptyState } from "@/components/ui/empty-state";
import { Icon } from "@/components/ui/icon";
import type { IcaiPublicCatalog, IcaiResourceType } from "@/lib/icai/types";
const TYPE_LABELS: Record<IcaiResourceType, string> = { rtp:"RTP", mtp:"MTP", study_material:"Study material", statutory_update:"Statutory update", amendment:"Amendment", question_paper:"Question paper", suggested_answer:"Suggested answer", schedule:"Schedule", announcement:"Announcement" };
function stamp(value:string){ return new Intl.DateTimeFormat("en-IN",{day:"numeric",month:"short",year:"numeric",timeZone:"UTC"}).format(new Date(value)); }
export function IcaiUpdatesFeed({catalog,viewerAttemptKey}:{catalog:IcaiPublicCatalog;viewerAttemptKey:string|null}){ const relevantResources=viewerAttemptKey?catalog.resources.filter((item)=>item.attemptKeys.includes(viewerAttemptKey)):[]; const relevantEvents=viewerAttemptKey?catalog.events.filter((item)=>item.attemptKey===viewerAttemptKey):[]; return <div className="icai-page"><section className="icai-hero"><div><Badge tone="brand">Daily verification feed</Badge><h1>ICAI Updates</h1><p>New and changed official ICAI resources are surfaced with provenance and verification timestamps.</p></div><div className="icai-verified-panel"><Icon name="bell" size={22}/><span><strong>Daily check</strong><small>Scheduled for 06:00 IST, independent of app visits.</small></span></div></section><section className="icai-notification-preview"><div><span className="eyebrow">Student notification preview</span><h2>{viewerAttemptKey?`Relevant to ${viewerAttemptKey}`:"Choose an attempt in your profile"}</h2><p>{viewerAttemptKey?`${relevantResources.length} resources and ${relevantEvents.length} verified exam events currently match your saved attempt.`:"When you are signed in with an attempt selected, CA Progress can preview which verified ICAI changes are relevant to you."}</p></div><Badge tone={viewerAttemptKey?"success":"neutral"}>{viewerAttemptKey?"Attempt-aware":"No attempt selected"}</Badge></section><form className="icai-filters icai-filters--updates" method="get"><label><span>Level</span><select name="level" defaultValue={catalog.filters.level}><option value="all">All levels</option>{catalog.levels.map((level)=><option key={level.code} value={level.code}>{level.name}</option>)}</select></label><label><span>Attempt</span><select name="attempt" defaultValue={catalog.filters.attempt}><option value="all">All attempts</option>{catalog.attempts.filter((attempt,index,rows)=>rows.findIndex((row)=>row.key===attempt.key)===index).map((attempt)=><option key={attempt.key} value={attempt.key}>{attempt.label}</option>)}</select></label><label><span>Type</span><select name="type" defaultValue={catalog.filters.type}><option value="all">All update types</option>{Object.entries(TYPE_LABELS).map(([key,label])=><option key={key} value={key}>{label}</option>)}</select></label><button className="ui-button ui-button--primary ui-button--md" type="submit"><span>Filter updates</span></button></form><section className="icai-section"><div className="icai-section-heading"><div><span className="eyebrow">Verified changes</span><h2>Latest official items</h2></div><Badge tone="info">{catalog.resources.length+catalog.events.length} items</Badge></div>{catalog.resources.length||catalog.events.length?<div className="icai-update-list">{catalog.events.slice(0,20).map((event)=><article key={event.id} className="icai-update-row icai-update-row--event"><span className="icai-update-icon"><Icon name="calendar"/></span><div><div className="icai-update-row__meta"><Badge tone="warning">Exam event</Badge><span>Verified {stamp(event.lastVerifiedAt)}</span></div><h3>{event.title}</h3><p>{event.attemptLabel} · {event.levelCode} · {stamp(event.eventDate)}</p></div><a href={event.sourceUrl} target="_blank" rel="noreferrer">Official source <Icon name="arrow" size={14}/></a></article>)}{catalog.resources.slice(0,80).map((resource)=><article key={resource.id} className="icai-update-row"><span className="icai-update-icon"><Icon name="bell"/></span><div><div className="icai-update-row__meta"><Badge tone="brand">{TYPE_LABELS[resource.type]}</Badge><span>Changed {stamp(resource.lastChangedAt)}</span><span>Verified {stamp(resource.lastVerifiedAt)}</span></div><h3>{resource.title}</h3><p>{resource.sourceName}{resource.attemptKeys.length?` · ${resource.attemptKeys.join(", ")}`:""}</p></div><a href={resource.officialUrl} target="_blank" rel="noreferrer">View official item <Icon name="arrow" size={14}/></a></article>)}</div>:<EmptyState icon="bell" title="No verified updates match these filters" description="The last verified dataset remains intact. Try another filter or check after the next scheduled sync."/>}</section></div>; }
