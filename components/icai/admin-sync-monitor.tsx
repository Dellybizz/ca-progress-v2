import { Badge } from "@/components/ui/badge";
import { EmptyState } from "@/components/ui/empty-state";
import { Icon } from "@/components/ui/icon";
import type { AppRole } from "@/lib/authorization/roles";
import type { IcaiAdminDashboard } from "@/lib/icai/types";
import { decideIcaiReviewAction, runIcaiSyncAction } from "@/app/(admin)/admin/icai-sync/actions";
function time(value:string|null){if(!value)return"Never";return new Intl.DateTimeFormat("en-IN",{day:"numeric",month:"short",hour:"2-digit",minute:"2-digit",timeZone:"Asia/Kolkata"}).format(new Date(value));}
export function IcaiAdminSyncMonitor({dashboard,role,notice,error}:{dashboard:IcaiAdminDashboard;role:AppRole;notice?:string|null;error?:string|null}){const run=dashboard.latestRun;return <div className="icai-page icai-admin-page"><section className="icai-hero"><div><Badge tone="warning">Admin · Phase 8</Badge><h1>ICAI Sync Monitor</h1><p>Monitor official-source health, review high-impact exam date changes and run the verification engine manually when needed.</p></div><form action={runIcaiSyncAction}><button type="submit" className="ui-button ui-button--primary ui-button--lg"><span>Run Sync now</span></button></form></section>{notice?<div className="auth-status auth-status--success" role="status">{notice}</div>:null}{error?<div className="auth-status auth-status--danger" role="alert">{error}</div>:null}<section className="icai-admin-summary"><div><span>Operator role</span><strong>{role.replaceAll("_"," ")}</strong></div><div><span>Schedule</span><strong>Daily · 06:00 IST</strong></div><div><span>Last run</span><strong>{run?time(run.startedAt):"No run yet"}</strong></div><div><span>Run status</span><strong>{run?.status??"Awaiting first run"}</strong></div></section>{run?<section className="icai-section"><div className="icai-section-heading"><div><span className="eyebrow">Latest run</span><h2>Verification results</h2></div><Badge tone={run.status==="success"?"success":run.status==="partial"?"warning":"danger"}>{run.status}</Badge></div><div className="icai-run-stats"><div><span>Sources</span><strong>{run.sourceSucceeded}/{run.sourceTotal}</strong></div><div><span>New</span><strong>{run.newItems}</strong></div><div><span>Changed</span><strong>{run.changedItems}</strong></div><div><span>Unchanged</span><strong>{run.unchangedItems}</strong></div><div><span>Removed links</span><strong>{run.removedItems}</strong></div><div><span>Review queue</span><strong>{run.pendingReviews}</strong></div></div>{run.errorSummary?<div className="icai-run-error"><Icon name="shield"/><span><strong>Run errors</strong><small>{run.errorSummary}</small></span></div>:null}</section>:<EmptyState icon="clock" title="No synchronization run has been recorded" description="The daily Cloudflare Cron Trigger or the manual Run Sync action will create the first run."/>}<section className="icai-section"><div className="icai-section-heading"><div><span className="eyebrow">Source registry</span><h2>Official source health</h2></div><Badge tone="neutral">{dashboard.sources.length} sources</Badge></div><div className="icai-source-table">{dashboard.sources.map((source)=><article key={source.id} className={source.failures?"has-error":""}><div><span className="icai-source-health"><i/>{source.name}</span><a href={source.officialUrl} target="_blank" rel="noreferrer">{source.officialUrl}</a></div><dl><div><dt>Last attempt</dt><dd>{time(source.lastAttemptAt)}</dd></div><div><dt>Last success</dt><dd>{time(source.lastSuccessAt)}</dd></div><div><dt>Failures</dt><dd>{source.failures}</dd></div></dl>{source.lastError?<p>{source.lastError}</p>:null}</article>)}</div></section><section className="icai-section"><div className="icai-section-heading"><div><span className="eyebrow">High-impact verification</span><h2>Review queue</h2></div><Badge tone={dashboard.reviews.length?"warning":"success"}>{dashboard.reviews.length} pending</Badge></div>{dashboard.reviews.length?<div className="icai-review-list">{dashboard.reviews.map((review)=><article key={review.id}><div><Badge tone="warning">{Math.round(review.confidence*100)}% parser confidence</Badge><h3>{review.title}</h3><p>{review.reason}</p><small>{review.sourceName} · detected {time(review.createdAt)}</small><a href={review.sourceUrl} target="_blank" rel="noreferrer">Inspect official source <Icon name="arrow" size={14}/></a></div><div className="icai-review-actions"><form action={decideIcaiReviewAction}><input type="hidden" name="reviewId" value={review.id}/><input type="hidden" name="decision" value="approved"/><button className="ui-button ui-button--primary ui-button--sm" type="submit"><span>Approve</span></button></form><form action={decideIcaiReviewAction}><input type="hidden" name="reviewId" value={review.id}/><input type="hidden" name="decision" value="rejected"/><button className="ui-button ui-button--secondary ui-button--sm" type="submit"><span>Reject</span></button></form></div></article>)}</div>:<EmptyState compact icon="check" title="No date changes need review" description="High-impact exam dates and times stay unchanged until an admin explicitly approves a detected change."/>}</section><section className="icai-section"><div className="icai-section-heading"><div><span className="eyebrow">Audit trail</span><h2>Recent detected changes</h2></div></div>{dashboard.recentChanges.length?<div className="icai-change-list">{dashboard.recentChanges.map((change)=><div key={change.id}><span><Badge tone={change.riskLevel==="high"?"warning":"neutral"}>{change.changeType}</Badge><strong>{change.entityType}</strong><small>{change.entityId}</small></span><span>{change.decisionStatus}<small>{time(change.detectedAt)}</small></span></div>)}</div>:<EmptyState compact icon="shield" title="No changes recorded yet" description="The audit trail begins when the first source sync detects a new, changed or removed item."/>}</section></div>;}
