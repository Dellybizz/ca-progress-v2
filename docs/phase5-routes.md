# Phase 5 routes

- `/progress` — private tracker with filters/search and auto-save.
- `/analytics` — private derived progress analytics.
- `/subjects/[subjectSlug]/progress` — private per-subject chapter tracker.
- `/api/progress` — authenticated stage/undo mutation endpoint.
