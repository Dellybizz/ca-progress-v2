# Phase 5 validation plan

Required gate: TypeScript, ESLint, Node tests, Next build and Cloudflare/OpenNext dry-run. Database acceptance is additionally checked in a rolled-back transaction against the isolated V2 Supabase project so no test progress remains behind.
