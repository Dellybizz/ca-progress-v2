# Progress history shape

Each event records user, chapter, stage, action, previous state, new state and time. Undo appends another event that references the reverted event; it does not erase history.
