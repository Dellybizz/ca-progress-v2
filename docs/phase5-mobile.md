# Phase 5 mobile behavior

The tracker switches from desktop row layout to chapter cards, keeps five compact stage controls horizontally available, makes save state sticky while edits are in flight, and collapses analytics into mobile-first cards. The existing primary mobile navigation already exposes Progress.
